# Product-management-system-
This web application manages a product inventory using HTML, CSS, and PHP/MySQL. index.html provides a styled form (style.css) to input product details. save.php captures this data using POST and inserts it into a MySQL database. view.php queries the database and displays all saved records in a structured HTML table.
